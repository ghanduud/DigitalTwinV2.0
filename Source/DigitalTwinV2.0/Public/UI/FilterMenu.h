// Remove these:
// UPROPERTY(meta = (BindWidget)) UCheckBox* AvailableCheck;
// UPROPERTY(meta = (BindWidget)) UCheckBox* SoldCheck;
// UPROPERTY(meta = (BindWidget)) UCheckBox* DiscountCheck;

protected:
    // Remove these:
    // UFUNCTION() void AvailableCheckChanged(bool bIsChecked);
    // UFUNCTION() void SoldCheckChanged(bool bIsChecked);
    // UFUNCTION() void DiscountCheckChanged(bool bIsChecked);